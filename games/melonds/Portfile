# -*- coding: utf-8; mode: tcl; tab-width: 4; indent-tabs-mode: nil; c-basic-offset: 4 -*- vim:fenc=utf-8:ft=tcl:et:sw=4:ts=4:sts=4

PortSystem          1.0
PortGroup           cmake 1.1
PortGroup           github 1.0

github.setup        Arisotura melonDS 0.9.1

name                melonds
platforms           darwin
license             GPL-3
maintainers         {gmail.com:audvare @Tatsh}
categories          games emulators
description         Nintendo DS emulator.
long_description    ${description}

checksums           rmd160  67fc0d51a6a6a82bef753718b8519042b526a1e3 \
                    sha256  79234895759aba878e2552cd8fab4685ae2b47908825f6efc3fd50f057486b5a \
                    size    1311147

depends_lib         port:libsdl2 \
                    port:libslirp \
                    port:glib2 \
                    port:gettext \
                    port:libarchive \
                    port:libiconv \
                    port:qt5-qtbase \
                    port:libepoxy

patchfiles      patch-1026.diff \
                patch-fix-scaling.diff
patch.pre_args  -p1

destroot {
    copy ${workpath}/build/melonDS.app ${destroot}${applications_dir}
}

if {${os.major} < 17} {
    pre-fetch {
        ui_error "${name} @${version} requires OS X 10.13 or later."
        return -code error "incompatible OS X version"
    }
}
