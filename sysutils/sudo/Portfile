# -*- coding: utf-8; mode: tcl; tab-width: 4; indent-tabs-mode: nil; c-basic-offset: 4 -*- vim:fenc=utf-8:ft=tcl:et:sw=4:ts=4:sts=4

PortSystem          1.0
PortGroup           github 1.0
PortGroup           xcode 1.0

github.setup        Tatsh sudo-touchid 87.140.1 v
name                sudo
categories          sysutils security
version             87.140.1
platforms           macosx
license             {AAPL MIT BSD}
maintainers         {gmail.com:audvare @Tatsh}

checksums           rmd160  50fa4bd61090aa1c8909a90e8b0cab7610b31480 \
                    sha256  3d3ca882be759eef098db14c93c7716b4ebf6f46312481f443a471296fdd5da7 \
                    size    3687525

homepage            https://opensource.apple.com/source/sudo/

livecheck.url       ${homepage}
livecheck.regex     "sudo-(\\d+(?:\\.\\d+(?:\\.\\d+)?)?)/"

description         Allow users to run programs as other users, e.g. root (with Touch ID patch).
long_description    ${description}

xcode.target        "Build All"
xcode.configuration Release

if {${os.major} < 15} {
    pre-fetch {
        ui_error "${name} @${version} requires OS X 10.11 or later."
        return -code error "incompatible OS X version"
    }
}

destroot {
    xinstall -m 111 ${worksrcpath}/build/Release/visudo ${destroot}${prefix}/sbin/visudo
    xinstall -m 4511 ${worksrcpath}/build/Release/sudo ${destroot}${prefix}/bin/sudo
}
